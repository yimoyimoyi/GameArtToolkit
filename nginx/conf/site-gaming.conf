# ==============================================================================
# GameArt Toolkit - 游戏生态全平台加速规则 (由 ServiceProfile 模板自动生成)
# ==============================================================================

# Steam 商店与结账
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name store.steampowered.com checkout.steampowered.com help.steampowered.com login.steampowered.com *.steampowered.com;

    location / {
        proxy_pass https://upstream_steam_store;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name "steambroadcast.akamaized.net";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Steam 社区与个人资料
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name steamcommunity.com api.steampowered.com *.steamcommunity.com;

    location / {
        proxy_pass https://upstream_steam_community;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host steamcommunity.com;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name "steambroadcast.akamaized.net";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_redirect default;
        proxy_redirect http:// https://;
        proxy_force_ranges on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Steam 静态图片 CDN
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name community.akamai.steamstatic.com avatars.akamai.steamstatic.com clan.akamai.steamstatic.com steamcommunity-a.akamaihd.net steamuserimages-a.akamaihd.net cdn.akamai.steamstatic.com community.cloudflare.steamstatic.com *.steamstatic.com;

    location / {
        proxy_pass https://upstream_steam_akamai;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name "steambroadcast.akamaized.net";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        # 开启本地磁盘缓存 (消除频次冲击与 0ms 秒开)
        proxy_cache pixiv_img_cache;
        proxy_cache_valid 200 304 7d;
        proxy_cache_valid 404 1m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_revalidate on;
        proxy_cache_lock on;
        add_header X-Cache-Status $upstream_cache_status;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Ubisoft 育碧商城
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name store.ubi.com ubisoftconnect.com api-ubiservices.ubi.com;

    location / {
        proxy_pass https://upstream_ubisoft;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# EA App / Origin
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name api.origin.com signin.ea.com api1.origin.com;

    location / {
        proxy_pass https://upstream_ea_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Battle.net 战网国际服
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name battle.net www.battle.net us.battle.net eu.battle.net kr.battle.net account.battle.net shop.battle.net blizzard.com www.blizzard.com us.cdn.blizzard.com level3.blizzard.com blznav.akamaized.net *.battle.net *.blizzard.com;

    location / {
        proxy_pass https://upstream_battle_net;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# GOG 游戏商城
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name gog.com www.gog.com api.gog.com login.gog.com images.gog.com;

    location / {
        proxy_pass https://upstream_gog;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Xbox 微软游戏生态
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name xbox.com www.xbox.com store.xbox.com support.xbox.com;

    location / {
        proxy_pass https://upstream_xbox;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Minecraft 游戏生态
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name minecraft.net www.minecraft.net account.mojang.com;

    location / {
        proxy_pass https://upstream_minecraft;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Accept-Encoding $http_accept_encoding;
        proxy_set_header Accept-Language $http_accept_language;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}
