# ==============================================================================
# PixivToolkit - 二次元与创作者生态加速规则配置 (参考 E:\pixiv-nginx 优化版)
# ==============================================================================

# Pixiv 主站、全子域与 APP API (采用空 SNI 策略，彻底绕过 GFW SNI 阻断)
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name www.pixiv.net pixiv.net *.pixiv.net ssl.pixiv.net accounts.pixiv.net touch.pixiv.net oauth.secure.pixiv.net app-api.pixiv.net dic.pixiv.net en-dic.pixiv.net sketch.pixiv.net payment.pixiv.net factory.pixiv.net comic.pixiv.net novel.pixiv.net imp.pixiv.net sensei.pixiv.net fanbox.pixiv.net source.pixiv.net i1.pixiv.net i2.pixiv.net i3.pixiv.net i4.pixiv.net;

    client_max_body_size 50M;

    location / {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /ajax/ {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_hide_header Access-Control-Allow-Origin;
        add_header Access-Control-Allow-Origin $http_origin always;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /ws/ {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Connection "upgrade";
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_hide_header Access-Control-Allow-Origin;
        add_header Access-Control-Allow-Origin $http_origin always;
        proxy_read_timeout 7200s;
        proxy_send_timeout 7200s;
    }
}

# Pixiv lc-event 专属流
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name lc-event.pixiv.net;

    location / {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# Pixiv pximg 插画原图 CDN (带本地图片高速磁盘缓存)
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name *.pximg.net s.pximg.net i.pximg.net imgaz.pixiv.net;

    location / {
        proxy_pass https://upstream_pixiv_img;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Referer "https://www.pixiv.net/";
        proxy_set_header Sec-Fetch-Site "cross-site";
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;

        proxy_cache pixiv_img_cache;
        proxy_cache_valid 200 304 30d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        add_header X-Cache-Status $upstream_cache_status;
        proxy_next_upstream_timeout 60;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# Pixiv Sketch 绘图直播流 CDN
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name *.pixivsketch.net;

    location / {
        proxy_pass https://upstream_pixiv_img;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;
    }
}

# Pixiv Fanbox 创作者赞助平台 (Cloudflare SNI 直连)
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name fanbox.cc *.fanbox.cc api.fanbox.cc downloads.fanbox.cc;

    client_max_body_size 50M;

    location / {
        proxy_pass https://upstream_pixiv_fanbox;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse off;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# BOOTH 同人商城
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name booth.pm *.booth.pm api.booth.pm assets.booth.pm;

    location / {
        proxy_pass https://upstream_booth_pm;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse off;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;
    }
}

# Danbooru 动漫图库
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name danbooru.donmai.us *.donmai.us cdn.donmai.us;

    location / {
        proxy_pass https://upstream_danbooru;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;

        proxy_cache pixiv_img_cache;
        proxy_cache_valid 200 304 7d;
        add_header X-Cache-Status $upstream_cache_status;
    }
}

# Yande.re 高清动漫壁纸
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name yande.re *.yande.re files.yande.re;

    location / {
        proxy_pass https://upstream_yandere;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;

        proxy_cache pixiv_img_cache;
        proxy_cache_valid 200 304 7d;
        add_header X-Cache-Status $upstream_cache_status;
    }
}

# VNDB 视觉小说库
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name vndb.org *.vndb.org t.vndb.org;

    location / {
        proxy_pass https://upstream_vndb;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;
    }
}

