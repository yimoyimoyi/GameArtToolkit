# ==============================================================================
# PixivToolkit - 二次元与创作者生态加速规则 (由 ServiceProfile 模板自动生成)
# ==============================================================================

# Pixiv 网页与 APP API (采用空 SNI 策略，绕过 GFW SNI 阻断)
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name pixiv.net www.pixiv.net ssl.pixiv.net accounts.pixiv.net touch.pixiv.net oauth.secure.pixiv.net dic.pixiv.net en-dic.pixiv.net sketch.pixiv.net payment.pixiv.net factory.pixiv.net comic.pixiv.net novel.pixiv.net imp.pixiv.net sensei.pixiv.net fanbox.pixiv.net source.pixiv.net i1.pixiv.net i2.pixiv.net i3.pixiv.net i4.pixiv.net app-api.pixiv.net;

    client_max_body_size 50M;

    location / {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /ajax/ {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_hide_header Access-Control-Allow-Origin;
        add_header Access-Control-Allow-Origin $http_origin always;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    location /ws/ {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Connection "upgrade";
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_hide_header Access-Control-Allow-Origin;
        add_header Access-Control-Allow-Origin $http_origin always;
        proxy_read_timeout 7200s;
        proxy_send_timeout 7200s;
    }
}

# Pixiv lc-event 专属流
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name lc-event.pixiv.net;

    location / {
        proxy_pass https://upstream_pixiv_web;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_max_temp_file_size 0;
        proxy_buffering off;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# Pixiv pximg 插画 CDN (带本地图片磁盘缓存与 Range 断点续传)
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name *.pximg.net s.pximg.net i.pximg.net imgaz.pixiv.net;

    location / {
        proxy_pass https://upstream_pixiv_img;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_set_header Referer "https://www.pixiv.net/";
        proxy_set_header Sec-Fetch-Site "cross-site";
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;

        proxy_cache pixiv_img_cache;
        proxy_cache_valid 200 304 30d;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_force_ranges on;
        add_header X-Cache-Status $upstream_cache_status;
        proxy_next_upstream_timeout 60;
        proxy_next_upstream error timeout http_429 http_404 http_500 http_502 http_503 http_504 non_idempotent;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# Pixiv Sketch 绘图直播流 CDN
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name *.pixivsketch.net;

    location / {
        proxy_pass https://upstream_pixiv_img;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header User-Agent $http_user_agent;
        proxy_ssl_name "";
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_next_upstream error timeout http_429 http_500 http_502 http_503 http_504;
    }
}

# Pixiv Fanbox 创作者赞助
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name fanbox.cc www.fanbox.cc api.fanbox.cc downloads.fanbox.cc *.fanbox.cc;

    client_max_body_size 50M;
    location / {
        proxy_pass https://upstream_pixiv_fanbox;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# BOOTH 同人商城
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name booth.pm www.booth.pm api.booth.pm assets.booth.pm *.booth.pm;

    client_max_body_size 50M;
    location / {
        proxy_pass https://upstream_booth_pm;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Danbooru 动漫图库
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name danbooru.donmai.us cdn.donmai.us;

    location / {
        proxy_pass https://upstream_danbooru;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Yande.re 高清动漫壁纸
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name yande.re www.yande.re files.yande.re assets.yande.re *.yande.re;

    location / {
        proxy_pass https://upstream_yandere;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_redirect default;
        proxy_redirect http:// https://;
        proxy_force_ranges on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# VNDB 视觉小说资料库
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name vndb.org t.vndb.org;

    location / {
        proxy_pass https://upstream_vndb;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}

# Fandom 游戏动漫社区 Wiki
server {
    listen 80;
    listen 443 ssl;
    http2 on;
    server_name fandom.com www.fandom.com wikia.com static.wikia.nocookie.net vignette.wikia.nocookie.net images.wikia.com services.fandom.com auth.fandom.com genshin-impact.fandom.com minecraft.fandom.com starwars.fandom.com zelda.fandom.com honkai-star-rail.fandom.com terraria.fandom.com *.fandom.com *.wikia.com *.nocookie.net;

    location / {
        proxy_pass https://upstream_fandom;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_ssl_name $host;
        proxy_ssl_server_name on;
        proxy_ssl_verify off;
        proxy_ssl_session_reuse on;
        proxy_redirect default;
        proxy_redirect http:// https://;
        proxy_force_ranges on;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
        proxy_next_upstream error timeout http_403 http_429 http_500 http_502 http_503 http_504 non_idempotent;
    }
}
